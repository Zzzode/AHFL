struct Request {
    value: String;
}

struct Context {
    value: String = "pending";
}

struct Response {
    value: String;
}

agent FinalHandlerMustReturnAgent {
    input: Request;
    context: Context;
    output: Response;
    states: [Init, Done];
    initial: Init;
    final: [Done];
    capabilities: [];
    transition Init -> Done;
}

flow for FinalHandlerMustReturnAgent {
    state Init {
        goto Done;
    }

    state Done {
        ctx.value = input.value;
    }
}
