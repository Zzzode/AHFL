module std::json;

import std::collections as collections;
import std::option as option;

pub type List<T> = collections::List<T>;
pub type Maybe<T> = option::Option<T>;

pub struct JsonArray {
    values: List<Int>;
}

pub struct JsonNumber {
    value: Maybe<Int>;
}
