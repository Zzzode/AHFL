module std::json;

import std::collections as collections;
import std::option as option;

type List<T> = collections::List<T>;
type Maybe<T> = option::Option<T>;

struct JsonArray {
    values: List<Int>;
}

struct JsonNumber {
    value: Maybe<Int>;
}
