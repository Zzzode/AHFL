module stdlib_units::option_neg_unknown;

import std::option as option;

// Negative (M0-3): calling a name that does not exist on std::option is an
// UNKNOWN_CALLABLE resolve error.
fn neg() -> Int effect Pure decreases 0 {
    let m = option::nonexistent<Int>(option::Option::Some(1));
    return 0;
}
