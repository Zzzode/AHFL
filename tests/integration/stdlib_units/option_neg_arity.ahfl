module stdlib_units::option_neg_arity;

import std::option as option;

// Negative (M0-3): map takes (self, f) — arity 2. Omitting the closure is a
// WRONG_ARITY error.
fn neg() -> Int effect Pure decreases 0 {
    let m = option::map<Int, Int>(option::Option::Some(1));
    return 0;
}
