module lib::agents;
import lib::types as types;

pub capability Echo(request: types::RequestAlias) -> types::ResponseAlias;

pub agent AliasAgent {
    input: types::RequestAlias;
    context: types::ContextAlias;
    output: types::ResponseAlias;
    states: [Init, Done];
    initial: Init;
    final: [Done];
    capabilities: [Echo];
    transition Init -> Done;
}
