module ir::workflow_value_flow;

pub struct Request {
    value: String;
}

pub struct Context {
    value: String = "seed";
}

pub struct Response {
    value: String;
}

pub agent EchoAgent {
    input: Request;
    context: Context;
    output: Response;
    states: [Init, Done];
    initial: Init;
    final: [Done];
    capabilities: [];
    transition Init -> Done;
}

flow for EchoAgent {
    state Init {
        goto Done;
    }

    state Done {
        return Response {
            value: input.value,
        };
    }
}

pub workflow ValueFlowWorkflow {
    input: Request;
    output: Response;

    node first: EchoAgent(input);
    node second: EchoAgent(Request { value: first.value }) after [first];

    return: Response { value: second.value };
}
