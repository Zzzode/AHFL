module typecheck::variance;
import typecheck::variance as self;
import std::collections;

const NarrowMapKey: collections::Map<String(2, 8), Int> =
    collections::map_empty<String(2, 8), Int>();
const RejectedMapKey: collections::Map<String, Int> = self::NarrowMapKey;
