module lib::one;
import lib::types as types;

pub capability Ping(request: types::OneReq) -> types::OneRes;

pub agent OneAgent {
    input: types::OneReq;
    context: types::OneCtx;
    output: types::OneRes;
    states: [Init, Done];
    initial: Init;
    final: [Done];
    capabilities: [Ping];
    transition Init -> Done;
}
