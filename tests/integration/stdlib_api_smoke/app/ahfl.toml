manifest_version = 1

[package]
name = "stdlib-api-smoke"
version = "0.1.0"
edition = "2026"
kind = "library"

[module]
prefix = "app"
root = "."

[exports]
modules = ["main"]

[targets.lib]
kind = "library"
entry = "main.ahfl"

[dependencies]
std = { source = "sysroot" }
