struct Request {
    value: String;
}

struct Context {
    value: String = "pending";
}

struct Response {
    value: String;
}

agent ReturnInNonFinalAgent {
    input: Request;
    context: Context;
    output: Response;
    states: [Init, Done];
    initial: Init;
    final: [Done];
    capabilities: [];
    transition Init -> Done;
}

flow for ReturnInNonFinalAgent {
    state Init {
        return Response {
            value: input.value,
        };
    }

    state Done {
        return Response {
            value: input.value,
        };
    }
}
